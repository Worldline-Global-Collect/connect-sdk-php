parameters:
  paths: ['lib', 'src']
  include: ['*.php']

  excludeProtected: false
  excludePrivate: true
  excludeTagged: ['internal']

  outputDir: '%workingDir%/docs'

  title: 'Worldline Connect PHP SDK'

  workerCount: 8
  memoryLimit: '512M'
